import template from './navigationBar.html';
import controller from './navigationBar.controller';

let navigationBarComponent = {
  restrict: 'E',
  bindings: {},
  template,
  controller
};

export default navigationBarComponent;